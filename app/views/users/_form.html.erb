<%= form_for @user do |t| %>
  Name
  <%= t.text_field :name %> 
  Email
  <%= t.text_field :email %>
  
  
  <%= t.submit %>
  <% @user.errors.full_messages.each do |msg| %>
  <li><%= msg %></li> 
  <% end %>
<% end %>

