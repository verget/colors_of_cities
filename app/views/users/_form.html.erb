<%= simple_form_for @user do |t| %>
  <%= t.input :name %> 
  <%= t.input :email %>  
  <%= t.button :submit %>
  <% @user.errors.full_messages.each do |msg| %>
  <li><%= msg %></li> 
  <% end %>
<% end %>

